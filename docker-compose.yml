version: "3.9"

services:
  app:
    build: .
    container_name: capyle_app
    volumes:
      - .:/app
    environment:
      DISPLAY: "host.docker.internal:0.0"
      PYTHONUNBUFFERED: "1"
    command: python3 CAPyle_releaseV2/release/main.py

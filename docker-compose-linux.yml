version: "3.9"

services:
  app:
    build: .
    container_name: capyle_app
    volumes:
      - .:/app
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      DISPLAY: ":0"
    command: python3 CAPyle_releaseV2/release/main.py
